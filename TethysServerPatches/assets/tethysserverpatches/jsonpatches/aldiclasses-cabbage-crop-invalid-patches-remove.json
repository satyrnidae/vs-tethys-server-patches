[
  // aldiclasses' cabbage crop patches are stale against the current game:cabbage.json shape:
  // cropsEarlyStage's two "move" ops (wrapping the addmerge that actually adds the seed drop)
  // reference /dropsByType/* which no longer resolves, and both of cropsDropRate's addmerge ops
  // reference /dropsByType/*-11 and /dropsByType/*-12 offsets that no longer exist. These fail
  // during path traversal itself (not a normal apply-time error), so an "enabled": false override
  // doesn't suppress them - the broken entries have to be removed from the patch array outright.
  // Remove from the highest index down within each file so earlier removals don't shift the
  // index of the next one. cropsEarlyStage's middle addmerge patch (index 1, which adds the
  // seeds-cabbage drop) still works correctly and is left in place.
  {
    "priority": 9999,
    "side": "server",
    "op": "Remove",
    "file": "aldiclasses:patches/cropsearlystage/survival-blocktypes-plant-crop-cabbage",
    "path": "/2",
    "dependsOn": [ { "modId": "aldiclasses" } ]
  },
  {
    "priority": 9999,
    "side": "server",
    "op": "Remove",
    "file": "aldiclasses:patches/cropsearlystage/survival-blocktypes-plant-crop-cabbage",
    "path": "/0",
    "dependsOn": [ { "modId": "aldiclasses" } ]
  },
  {
    "priority": 9999,
    "side": "server",
    "op": "Remove",
    "file": "aldiclasses:patches/cropsdroprate/survival-blocktypes-plant-crop-cabbage",
    "path": "/1",
    "dependsOn": [ { "modId": "aldiclasses" } ]
  },
  {
    "priority": 9999,
    "side": "server",
    "op": "Remove",
    "file": "aldiclasses:patches/cropsdroprate/survival-blocktypes-plant-crop-cabbage",
    "path": "/0",
    "dependsOn": [ { "modId": "aldiclasses" } ]
  }
]
